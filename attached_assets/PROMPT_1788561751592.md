# Replit Development Prompt: Google Calendar & Cloudinary Integration

You're working on a React + Vite + Express project (gicmasjid.org). The project uses:
- **Framework**: Vite + React with Express backend
- **UI**: Radix UI components + Tailwind CSS v4
- **Routing**: Wouter
- **File structure**: `client/src/pages/` for pages, `client/src/components/ui/` for UI components

---

## Task 1: Create Google Calendar Integration at `/sandbox/calendar`

### Step 1: Install FullCalendar packages

Run this command in your terminal:

```bash
npm install @fullcalendar/react @fullcalendar/daygrid @fullcalendar/timegrid @fullcalendar/interaction @fullcalendar/google-calendar
```

### Step 2: Create file `client/src/pages/SandboxCalendar.tsx`

This page should:
- Display FullCalendar in month view
- Sync with Google Calendar email: `adilshaikh1608@gmail.com`
- Use Google Calendar API Key: `AIzaSyCn00fYWfmxHLEBMJ8MuTh_dDLszCdLZ-Y`
- Match the existing Tailwind + Radix UI theme (use the same colors/spacing as the rest of the site)
- Allow users to click events to see details
- Include custom styling to match the mosque website aesthetic (professional, clean, with brown/Islamic tones if visible in your design)
- Make it responsive for mobile and desktop

**Key requirements:**
- Import `@fullcalendar/react`, `@fullcalendar/daygrid`, `@fullcalendar/google-calendar`
- Use `headerToolbar` with navigation (prev, next, today) and title
- Set `initialView="dayGridMonth"`
- Use `googleCalendarApiKey` with the provided API key
- Use `events` prop with `googleCalendarId: 'adilshaikh1608@gmail.com'`
- Add custom CSS classes for `eventClassNames` and `dayCellClassNames` to style consistently
- Wrap in a container with padding/margin matching your site's design

### Step 3: Update routing in `client/src/App.tsx`

Add this route to your App.tsx file (look for other Route imports and add it with them):

```tsx
<Route path="/sandbox/calendar" component={SandboxCalendar} />
```

Make sure to import the component at the top:
```tsx
import SandboxCalendar from '@/pages/SandboxCalendar';
```

---

## Task 2: Create Cloudinary Assets Gallery at `/sandbox/assets`

### Step 1: Create file `client/src/pages/SandboxAssets.tsx`

This page should:
- Load images from Cloudinary
- Cloudinary Cloud Name: `kphs2hv6`
- Cloudinary Folder: `custom`
- Display as a responsive **grid gallery** (3-4 columns on desktop, 2 on tablet, 1 on mobile)
- Use Cloudinary's URL format: `https://res.cloudinary.com/kphs2hv6/image/list/custom.json`
- Show images with proper aspect ratio and lazy loading
- Match the existing Tailwind + Radix UI theme (same spacing, colors, shadows as rest of site)
- Add smooth hover effects (scale, shadow) using Tailwind transitions
- Include loading state while fetching images

**Key requirements:**
- Use `fetch()` to get image list from Cloudinary API
- Parse the JSON response which returns `resources` array
- Map over `resources` and display each with `secure_url`
- Use Tailwind classes for grid: `grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4`
- Use `<img>` tags with `alt` text for each image
- Add `loading="lazy"` for performance
- Add hover effects: `hover:scale-105 transition-transform duration-300`
- Add error handling if API fails

### Step 2: Update routing in `client/src/App.tsx`

Add this route to your App.tsx file:

```tsx
<Route path="/sandbox/assets" component={SandboxAssets} />
```

Make sure to import the component at the top:
```tsx
import SandboxAssets from '@/pages/SandboxAssets';
```

---

## Task 3: Styling Guidelines

Both pages should:
- Use Tailwind CSS classes (no hardcoded colors)
- Match the existing site's color scheme (check Footer.tsx, Navbar.tsx for reference)
- Use consistent spacing: `p-4`, `p-6`, `mb-4`, etc.
- Include a page title/header at the top
- Wrap content in a container with `max-w-7xl mx-auto`
- Ensure both pages have proper padding on mobile (`px-4`)
- Use Radix UI Button component from `@/components/ui/button` if you need any buttons
- Use Radix UI Card component from `@/components/ui/card` if you want card styling

---

## Task 4: Code Structure Hints

### For SandboxCalendar.tsx, your component structure should be:

```
- Import FullCalendar and plugins from @fullcalendar packages
- Create React functional component
- Add custom CSS variables for calendar theming (colors, fonts)
- Return JSX with:
  - Page title/header
  - Container div with max-w-7xl and padding
  - <FullCalendar /> component with all required props
- Export default component
```

### For SandboxAssets.tsx, your component structure should be:

```
- Import useState, useEffect from React
- Create React functional component
- Add state for:
  - images: array of image objects from Cloudinary
  - loading: boolean for loading state
  - error: string for error messages
- useEffect hook to fetch from Cloudinary API on mount:
  - Fetch from https://res.cloudinary.com/kphs2hv6/image/list/custom.json
  - Parse JSON response
  - Extract resources array and set to state
  - Handle errors gracefully
- Return JSX with:
  - Page title/header
  - Container div with max-w-7xl and padding
  - Loading spinner/message if loading
  - Error message if error occurs
  - Responsive grid of images if loaded successfully
    - Grid: grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4
    - Each image in a card with hover effects
- Export default component
```

---

## Task 5: Testing

After creating both files and updating App.tsx:

1. Run `npm run dev` to start the dev server
2. Navigate to `/sandbox/calendar` and verify:
   - Google Calendar loads
   - Events from adilshaikh1608@gmail.com display
   - Month view is visible with navigation buttons
   - Can click on events to see details
3. Navigate to `/sandbox/assets` and verify:
   - Images from Cloudinary load in a grid
   - Grid is responsive (changes columns on mobile/tablet/desktop)
   - Images have smooth hover effects
   - Loading state works
4. Test on mobile/tablet responsiveness
5. Verify styling matches the rest of the website

---

## Additional Notes

- Both routes are prefixed with `/sandbox/` for isolation/testing
- No environment variables needed - API key is hardcoded for now
- FullCalendar uses Tailwind CSS for styling by default, customize as needed
- Cloudinary API returns public images only (no authentication required for public folders)
- If images don't load from Cloudinary, verify the cloud name and folder name are correct

Good luck! Let me know if you hit any issues.
